---
interface Props {
  id: number;
  name: string;
  price: number;
  image: string;
}

const { id, name, price, image } = Astro.props;
---

<div class="brutalist-border p-4 bg-kaya-dark hover:bg-kaya-dark/90 transition-all">
  <a href={`/galeria/producto/${id}`} class="block">
    <div class="relative aspect-square overflow-hidden mb-4">
      <img 
        src={image} 
        alt={name}
        class="w-full h-full object-cover transform hover:scale-105 transition-transform duration-300"
      />
    </div>
    <h3 class="text-xl font-bold mb-2">{name}</h3>
    <p class="font-mono text-kaya-accent">${price}</p>
  </a>
  <div class="flex gap-2 mt-4">
    <a 
      href={`/galeria/producto/${id}`}
      class="brutalist-border flex-1 py-2 hover:text-kaya-accent transition-colors inline-block text-center"
    >
      VER PRODUCTO
    </a>
    <button 
      onclick={`addToCart({id: ${id}, name: '${name}', price: ${price}})`}
      class="brutalist-border flex-1 py-2 hover:text-kaya-accent transition-colors inline-block text-center"
    >
      AGREGAR AL CARRITO
    </button>
  </div>
</div>